# --- superpowers (plans, scratch, skill state) — KEEP LOCAL, NEVER UPLOAD ---
superpowers/
.superpowers/
docs/superpowers/
**/superpowers/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/
.env

# --- macOS ---
.DS_Store

# --- Nextflow / pipeline runs (never commit run artifacts) ---
.nextflow/
.nextflow.log*
work/
results/
*.pyc

# per-version cache: materialized worktrees + on-demand docs for non-pinned releases
pipelines/*/.versions/

# git worktrees (parallel agents)
.worktrees/
